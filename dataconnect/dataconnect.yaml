specVersion: "v1"
serviceId: "taskorganizer"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "taskorganizer-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
